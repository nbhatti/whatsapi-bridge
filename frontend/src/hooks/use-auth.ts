// Re-export auth context and types for convenience
export { useAuth, AuthProvider } from '../contexts/auth-context'

// Export types for use in components
export type { User, LoginCredentials } from '../contexts/auth-context'
